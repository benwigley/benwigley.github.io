jQuery(document).ready(function($) {


	// Button select
	// -------------

	$('select.button-select.custom').buttonSelect({
		'buttonClass': 'button'
	});

	$('select.button-select.bootstrap').buttonSelect({
		'buttonClass': 'btn'
	});


	// Scrollbrakes
	// -------------

	// $('.scrollbrakes').scrollbrakes();


	// Input resizer
	// -------------

	// $('.input-rezieable').inputResizer();

});